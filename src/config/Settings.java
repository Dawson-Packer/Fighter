package src.config;

import interfaces.config.PlayerSettings;
import interfaces.config.WindowSettings;
import interfaces.config.FieldSettings;

public class Settings implements PlayerSettings, WindowSettings, FieldSettings {

    public player_status player_status;

    public Settings() { }
}
